import { NumberDiffPipe } from './number-diff.pipe';

describe('NumberDiffPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberDiffPipe();
    expect(pipe).toBeTruthy();
  });
});
